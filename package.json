{
  "name": "wethru-profile-customers",
  "version": "0.1.0",
  "description": "WeThru Profile Factory — online business profiles for WeThru customers",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "new:profile": "node scripts/new-profile.mjs",
    "validate:customers": "node scripts/validate-customers.mjs",
    "domains:map": "node scripts/generate-domain-map.mjs"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "typescript": "^5.6.0"
  }
}
